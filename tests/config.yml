config:
  target: 'http://localhost:3000'   # Your locally hosted app
  phases:
    - duration: 60  # Test duration in seconds
      arrivalRate: 5  # Number of virtual users per second
scenarios:
  - flow:
      - get:
          url: '/'  # The route you want to test
