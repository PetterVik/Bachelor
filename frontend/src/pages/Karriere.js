import React from "react";
import "../styles.css"; // Correct relative path

// Define the "Karriere" component
const Karriere = () => {
  return <h1>Karriere</h1>;
};

// Export the About component so it can be used in routing
export default Karriere;
