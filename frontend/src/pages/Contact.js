import React from "react";

// Define the Contact component
const Contact = () => {
  return <h1>Contact Page</h1>;
};

// Export the Contact component so it can be used in routing
export default Contact;
